# ESLint 10 is ahead of some plugins' declared peer ranges (e.g.
# eslint-plugin-jsx-a11y still lists <=9, but works with 10). Use legacy peer
# resolution so local installs and CI `npm ci` agree. Revisit when plugins
# catch up to ESLint 10. (Mirrors apex-admin.)
legacy-peer-deps=true
